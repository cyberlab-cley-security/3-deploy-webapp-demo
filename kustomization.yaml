apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Reference the deployment manifest
resources:
  - deployment.yaml
# Dynamically set the Docker image name and tag
images:
  - name: IMAGE_PLACEHOLDER # Matches placeholder in deployment.yaml
    newName: "016246143337.dkr.ecr.us-east-1.amazonaws.com/spoon-chrisley75-ecr-q2h" # Can be changed dynamically
    newTag: "28" # Can be updated via CI/CD
